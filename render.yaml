services:
  - type: web
    name: egress-backend-api
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python seed_code_clauses.py
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        value: postgresql://postgres.xtppmnmpunfflnxrfinb:a%26WHy9%25K48Yas3M@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres
      - key: FLS_DATABASE_PATH
        value: data/fls_demo.db
